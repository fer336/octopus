# Server para CMS/Admin - cms-octopus.qeva.xyz
server {
  listen 80;
  server_name cms-octopus.qeva.xyz;

  root /usr/share/nginx/html;
  index admin.html;

  # Servir admin.html
  location = /admin.html {
    try_files $uri =404;
  }

  # Assets
  location /assets/ {
    try_files $uri =404;
    add_header Cache-Control "no-cache";
  }

  # Root - sirven admin.html
  location = / {
    try_files $uri /admin.html =404;
  }

  # SPA routing
  location / {
    try_files $uri $uri/ /admin.html =404;
  }

  # Proxy al backend para API
  location /api/ {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Proxy al backend para Auth
  location /auth/ {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# Server para Sistema/Tenant - octopus.qeva.xyz
server {
  listen 80;
  server_name octopus.qeva.xyz;

  root /usr/share/nginx/html;
  index tenant.html;

  # Servir tenant.html
  location = /tenant.html {
    try_files $uri =404;
  }

  # Landing
  location = /landing.html {
    try_files $uri =404;
  }

  # Assets
  location /assets/ {
    try_files $uri =404;
    add_header Cache-Control "no-cache";
  }

  # Root - sirven tenant.html
  location = / {
    try_files $uri /tenant.html =404;
  }

  # SPA routing
  location / {
    try_files $uri $uri/ /tenant.html =404;
  }

  # Proxy al backend para API
  location /api/ {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Proxy al backend para Auth
  location /auth/ {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# Server para API - api.octopus.qeva.xyz
server {
  listen 80;
  server_name api.octopus.qeva.xyz;

  location / {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# Fallback server
server {
  listen 80 default_server;
  server_name _;

  root /usr/share/nginx/html;
  index tenant.html;

  location / {
    try_files $uri $uri/ /tenant.html =404;
  }

  location /api/ {
    proxy_pass http://octopus_backend:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
  }
}